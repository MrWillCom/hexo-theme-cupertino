<div class="container">
  <% if (page.path == "index.html") { %>
  <div class="hero<% if (theme.hero_exit_while_scrolling) { %> exit-while-scroll<% } %>">
    <h2 class="sub"><%- config.subtitle %></h2>
    <h2 class="large"><%- config.description %></h2>
  </div>

  <h2 class="section-title"><%- __('latest_posts') %></h2>
  <% } %>

  <%- partial('_partial/post-list', { posts: page.posts }) %>

  <%- partial('_partial/paginator') %>
</div>
