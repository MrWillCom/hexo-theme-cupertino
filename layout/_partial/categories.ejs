<div class="container">
  <div class="meta">
    <h1 class="title"><%= __('categories') %></h1>
  </div>
  <div class="categories">
    <% site.categories.forEach(category => { %>
    <a href="<%- url_for(category.path) %>" class="category"><%- category.name %><% if (theme.categories_post_count) { %><span class="count"><%- category.length %></span><% } %></a>
    <% }) %>
  </div>
</div>
